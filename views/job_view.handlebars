<div id="job-view-container" class="container">
  <div class="row text-center">
    <a href="/" class="back-link main-color">Voltar</a>
    <div id="img-container" class="col-md-12">
      <img src="/images/company.svg" alt="Company">
    </div>
    <div class="col-md-8 mx-md-auto">
      <h1>{{job.dataValues.title}}</h1>
      <p class="bold">Quem eles estão buscando:</p>
      <p class="job-description">{{job.dataValues.description}}</p>
      <p>
        <span class="bold">O salário informado é de:</span> R$ {{job.dataValues.salary}}
      </p>
      <p>Para trabalhar na
        <span class="bold main-color">{{job.dataValues.company}}</span>
        envie um email para
        <a href="mail:to{{job.dataValues.email}}" class="main-color">
          {{job.dataValues.email}}
        </a>
      </p>
    </div>
  </div>
</div>